name: turbo-flutter
description: An ultra wide turbo turbo-flutter brick.
version: 0.1.0+1
environment:
  mason: ^0.1.0
vars:
  packageName:
    type: string
    description: The package name (e.g. ultra_wide_turbo_bricks -> import 'package:ultra_wide_turbo_bricks..)
    prompt: What is the package name?
